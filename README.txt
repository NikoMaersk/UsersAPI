Seed data for admin and names collection are being created through the docker-compose file.
Check ".\seed_data\import.log" incase of failure.

To initialize seed data for users navigate to ".\seed_data\" and run "python init.py" in terminal.